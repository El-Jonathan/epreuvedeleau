// parametre à l'envers

let phr = `"codeur" "je" "suis"`;

function reverse (){

let envers = phr.split(" ").reverse();
    console.log(envers);

envers.forEach(mots => console.log(mots)){
    if (mots == undefined){
        (console.log("error"))
        return;
    };
}
}
